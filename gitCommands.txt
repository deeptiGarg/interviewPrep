Using GIT CMD - windows

To check settings-
git config --list

To clone a repo
git clone <repo-url>

To check if local repo is up-to-date
git status

To add a new change/new file to the next commit
git add <fileName>

To commit
git commit -m "commit message"

To push changes
git push

To create a new repo - 
1. go to the working directory on your local disk in git cmd. 
git init

2. add files -
git add .      //all files
git add <fileName>	

3. git commit -m "First commit"

4. Go to github and create a repo from the browser.

5. git remote add origin https://github.com/deeptiGarg/to-do-list.git

6. git push origin master

To remove any file from repo - 
git rm <fileName>


TO PULL from a branch
git pull origin testBranch


git clone https://github.com/NervanaSystems/private-nlp-architect.git

git pull origin lexicon_manager



To check branch —

git branch

To do a hard reset —
git reset -hard

To Fetch the latest changes - 
git fetch origin 

To create a copy of a branch on my local pc — 
git checkout -b lexicon_branch origin/lexicon_branch



Create my own branch/ fork a new branch—
Cd ..

To commit to a new branch — 
git commit -m "Test commit"


To push changes to a new branch —
git push origin dp/testBranch1


